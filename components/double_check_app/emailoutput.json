in this map i want to filter the links for only link string with https://

{urlResults.WebCrawlResponse.image_links.map(
                    (webCrawl, index) => (
                      <ul key={index}>
                        <a
                          href={webCrawl.url}
                          className="text-gray-800 link"
                          target="_blank"
                          rel="noopener noreferrer"
                        >
                          <li>{webCrawl.url}</li>
                        </a>
                      </ul>
                    ),
                  )}